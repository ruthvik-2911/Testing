server.port=${SERVER_PORT:8081}
mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/mobilize}
mongodb.database=mobilize
spring.data.mongodb.uri=${mongodb.uri}
spring.data.mongodb.database=mobilize

aws.accessKey=${AWS_ACCESS_KEY:}
aws.secretKey=${AWS_SECRET_KEY:}
aws.region=${AWS_REGION:ap-south-1}
aws.disableEc2Metadata=true
aws.s3.bucketName=${AWS_S3_BUCKET_NAME:keliri}
aws.s3.endpointUrl=${AWS_S3_ENDPOINT_URL:https://keliri.s3.ap-south-1.amazonaws.com}
aws.s3.folder=${AWS_S3_FOLDER:media}
aws.s3.thumbnailFolder=${AWS_S3_THUMBNAIL_FOLDER:thumbnails}

apk.file.path=${APK_FILE_PATH:}

# ── JWT & Security Configuration ──────────────────────────────────────────────
security.jwt.secret=${JWT_SECRET:keliri-superadmin-secret-key-must-be-32-chars}
security.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
security.superadmin.lock-duration-ms=${SUPERADMIN_LOCK_DURATION_MS:900000}
security.superadmin.max-failed-attempts=${MAX_FAILED_ATTEMPTS:5}
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175,http://127.0.0.1:5173,http://127.0.0.1:5174,http://127.0.0.1:5175}

# Firebase Admin Credentials
firebase.project-id=${FIREBASE_PROJECT_ID:consultancy-platform-2d615}
firebase.client-email=${FIREBASE_CLIENT_EMAIL:firebase-adminsdk-fbsvc@consultancy-platform-2d615.iam.gserviceaccount.com}
firebase.private-key=${FIREBASE_PRIVATE_KEY:YOUR_FIREBASE_PRIVATE_KEY_HERE}

# ── Gmail SMTP ────────────────────────────────────────────────────────────────
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USER:sonuayadavsk@gmail.com}
spring.mail.password=${EMAIL_PASS:YOUR_EMAIL_PASS_HERE}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
app.mail.from=${EMAIL_USER:sonuayadavsk@gmail.com}
app.mail.from-name=Keliri Platform

# ── Super Admin Portal ─────────────────────────────────────────────────────────
# Change this to your deployed URL when hosting on AWS
app.superadmin.portal-url=${SUPERADMIN_PORTAL_URL:http://localhost:5175}
app.superadmin.master-email=${MASTER_SUPERADMIN_EMAIL:sonuayadavsk@gmail.com}

# ── Mobilize API Integration ───────────────────────────────────────────────────
mobilize.api.url=${MOBILIZE_API_URL:http://15.206.186.192:3000/v1}
mobilize.api.master-email=${MOBILIZE_MASTER_EMAIL:admin@keliri.com}
mobilize.api.master-password=${MOBILIZE_MASTER_PASSWORD:Password@123}

# ── Razorpay Configuration ──────────────────────────────────────────────────────
razorpay.key.id=${RAZORPAY_KEY_ID:YOUR_RAZORPAY_KEY_ID_HERE}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:YOUR_RAZORPAY_KEY_SECRET_HERE}
razorpay.currency=INR

# ── File Upload Configuration ───────────────────────────────────────────────────
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=25MB